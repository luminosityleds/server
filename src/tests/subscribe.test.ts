// TODO: Add subscribe test

// Can be used to describe properties of Login
describe('Subscribe Test', () => {
    test('Subscribe', () => {
        let sub = 'Subscribe!'
        expect(sub).toBe(sub);
      });
});
